<h1><%= @question.title %></h1>

<%= link_to "Edit", edit_question_path(@question), 
    class: 'btn btn-success' %>

<p><%= @question.body %></p>
<p>resolved: <%= @question.resolved ? "✓"  : "no" %></p>

<%= link_to "Delete", @question, method: :delete, 
    class: 'btn btn-success' %>